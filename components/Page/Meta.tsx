import Head from 'next/head';

const Meta: React.FunctionComponent = () => (
  <Head>
    <meta name='viewport' content='width=device-width,initial-scale=1' />
    <title>Łukasz Myśliwiec 👋</title>
  </Head>
);

export default Meta;
